name: Scheduled Euromilhoes Script

on:
  schedule:
<<<<<<< HEAD
    - cron: "5 0-23 * * *"  # Runs every hour at the 5th minute
=======
    - cron: "5 0-23 * * *"  # Runs every hour at the 5th minute of the hour
>>>>>>> 3d7a760a8755d8d9d3c011dac9578f8f1203ef7b

jobs:
  run_script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt  # Ensure you have all dependencies in requirements.txt

      - name: Check Environment Variables
        run: |
          echo "Twilio Account SID: ${{ secrets.TWILIO_ACCOUNT_SID }}"
          echo "From WhatsApp Number: ${{ secrets.WHATSAPP_FROM_NUMBER }}"

      - name: Run Python script
        env:
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
          TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
          WHATSAPP_FROM_NUMBER: ${{ secrets.WHATSAPP_FROM_NUMBER }}
          WHATSAPP_TO_NUMBER: ${{ secrets.WHATSAPP_TO_NUMBER }}
        run: |
          python automation.py